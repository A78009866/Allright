<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <style>
        :root {
            --main-bg: #121212;
            --card-bg: #1e1e1e;
            --text-color: #ffffff;
            --primary-color: #bb86fc; 
            --secondary-color: #03dac6;
            --error-color: #cf6679;
            --success-color: #4CAF50;
        }
        body { 
            font-family: Tahoma, Arial, sans-serif; 
            background-color: var(--main-bg); 
            color: var(--text-color); 
            margin: 0; 
            padding: 0; 
        }
        header { 
            background-color: #000; 
            padding: 1.5em 5%; 
            display: flex; 
            justify-content: space-between; 
            align-items: center;
            border-bottom: 1px solid #333;
        }
        header h1 { 
            margin: 0; 
            font-size: 2em; 
            color: var(--primary-color);
        }
        nav a { 
            color: var(--secondary-color); 
            text-decoration: none; 
            margin-left: 20px; 
            font-weight: bold; 
            transition: color 0.3s;
        }
        nav a:hover {
            color: var(--primary-color);
        }
        main { 
            padding: 30px 5%; 
            max-width: 700px; 
            margin: 30px auto; 
            background-color: var(--card-bg); 
            border-radius: 12px; 
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.4); 
        }
        h2 { 
            border-bottom: 2px solid var(--primary-color); 
            padding-bottom: 10px; 
            margin-bottom: 30px;
            color: var(--primary-color);
        }
        form div { 
            margin-bottom: 20px; 
        }
        label { 
            display: block; 
            margin-bottom: 8px; 
            font-weight: bold; 
            color: #ccc; 
        }
        input[type="text"], input[type="url"], textarea {
            width: 100%;
            padding: 12px;
            border: 1px solid #555;
            border-radius: 6px;
            box-sizing: border-box; 
            background-color: #2a2a2a;
            color: var(--text-color);
            font-size: 1em;
        }
        textarea { 
            resize: vertical; 
            height: 120px; 
        }
        button[type="submit"] {
            background-color: var(--secondary-color);
            color: #000;
            padding: 12px 25px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 1.1em;
            font-weight: bold;
            transition: background-color 0.3s;
        }
        button[type="submit"]:hover { 
            background-color: var(--primary-color);
            color: var(--text-color);
        }
        .message-box {
            padding: 15px;
            border-radius: 6px;
            margin-bottom: 20px;
            font-weight: bold;
            text-align: center;
        }
        .error-message { 
            background-color: #4a1c22; 
            color: var(--error-color); 
            border: 1px solid var(--error-color);
        }
        .success-message { 
            background-color: #2a412a; 
            color: var(--success-color); 
            border: 1px solid var(--success-color);
        }
        footer { 
            text-align: center; 
            padding: 15px; 
            background-color: #000; 
            color: #888; 
            margin-top: 50px; 
            font-size: 0.9em;
        }
    </style>
</head>
<body>

    <header>
        <h1>🔐 <span style="color: var(--secondary-color);">Bac</span>Tube Admin</h1>
        <nav>
            <a href="/">عرض الفيديوهات</a>
        </nav>
    </header>

    <main>
        <h2>إضافة فيديو دراسي جديد</h2>

        <% if (message) { %>
            <div class="message-box <%= messageType === 'error' ? 'error-message' : 'success-message' %>">
                <%= message %>
            </div>
        <% } %>

        <form action="/admin" method="POST">
            <div>
                <label for="title">عنوان الفيديو:</label>
                <input type="text" id="title" name="title" required>
            </div>
            
            <div>
                <label for="youtubeUrl">رابط فيديو يوتيوب:</label>
                <input type="url" id="youtubeUrl" name="youtubeUrl" placeholder="https://www.youtube.com/watch?v=..." required>
            </div>
            
            <div>
                <label for="description">الوصف (اختياري):</label>
                <textarea id="description" name="description"></textarea>
            </div>
            
            <button type="submit">إضافة الفيديو</button>
        </form>
    </main>

    <footer>
        <p>⚠️ هذه الصفحة خاصة بالأدمن ويجب تأمينها بشكل صحيح في بيئة الإنتاج. | &copy; BacTube.</p>
    </footer>

</body>
</html>
