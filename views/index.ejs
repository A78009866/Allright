<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <style>
        :root {
            --bg-color: #000000;
            --glass-bg: rgba(255, 255, 255, 0.08); 
            --border-color: rgba(255, 255, 255, 0.15); 
            --text-color: #ffffff; 
            --logo-color: #ffffff;
            --primary-color: #8be9fd; /* لون إضاءة مميز (سايان) */
        }
        body { 
            font-family: Tahoma, Arial, sans-serif; 
            background-color: var(--bg-color); 
            color: var(--text-color); 
            margin: 0; 
            padding: 0; 
            background-image: linear-gradient(135deg, #0f0f0f 0%, #000000 100%);
            min-height: 100vh;
        }
        header { 
            background-color: var(--glass-bg); 
            backdrop-filter: blur(10px);
            padding: 1.5em 5%; 
            display: flex; 
            justify-content: flex-start; /* تم تعديل هذا ليناسب عدم وجود روابط أخرى */
            align-items: center;
            border-bottom: 1px solid var(--border-color);
            position: sticky;
            top: 0;
            z-index: 100;
        }
        header h1 { 
            margin: 0; 
            font-size: 2.2em; /* تكبير الشعار قليلاً */
            color: var(--logo-color);
        }
        /* إزالة أنماط Nav لأنها فارغة الآن */
        main { 
            padding: 30px 5%; 
            max-width: 1400px; 
            margin: 0 auto; 
        }
        h2 { 
            border-bottom: 2px solid var(--primary-color); 
            padding-bottom: 10px; 
            margin-bottom: 30px;
            color: var(--text-color); 
            font-size: 1.8em;
        }
        /* تحسينات عرض الشبكة */
        .video-grid { 
            display: grid; 
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr)); /* زيادة الحد الأدنى للعرض */
            gap: 40px; 
        }
        /* بطاقة الفيديو بتصميم زجاجي */
        .video-card { 
            background: var(--glass-bg); 
            backdrop-filter: blur(8px);
            border-radius: 18px; 
            border: 1px solid var(--border-color);
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.2); 
            overflow: hidden; 
            transition: transform 0.3s, box-shadow 0.3s;
        }
        .video-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 10px 40px rgba(139, 233, 253, 0.2), 0 0 15px rgba(139, 233, 253, 0.4); /* توهج واضح */
        }
        .video-card h3 { 
            background-color: rgba(255, 255, 255, 0.05); 
            color: var(--primary-color); /* لون العنوان بلون الإضاءة */
            margin: 0; 
            padding: 15px; 
            font-size: 1.4em; /* تكبير العنوان */
            border-bottom: 1px solid var(--border-color);
        }
        .video-player { 
            position: relative; 
            width: 100%; 
            padding-top: 56.25%; 
        }
        .video-player iframe { 
            position: absolute; 
            top: 0; 
            left: 0; 
            width: 100%; 
            height: 100%; 
            border: none; 
        }
        .video-card p { 
            padding: 15px; 
            color: #ddd; 
            font-size: 1em; 
            line-height: 1.6;
        }
        footer { 
            text-align: center; 
            padding: 20px; 
            background-color: var(--glass-bg); 
            backdrop-filter: blur(10px);
            color: #ccc; 
            margin-top: 50px; 
            font-size: 0.9em;
            border-top: 1px solid var(--border-color);
        }
    </style>
</head>
<body>

    <header>
        <h1><span style="color: var(--primary-color);">Bac</span><span style="color: var(--logo-color);">Tube</span></h1>
        
        </header>

    <main>
        <h2></h2>

        <div class="video-grid">
            <% if (videos && videos.length > 0) { %>
                <% videos.forEach(video => { %>
                    <div class="video-card">
                        <h3><%= video.title %></h3>
                        
                        <div class="video-player">
                            <iframe 
                                src="https://www.youtube.com/embed/<%= video.videoId %>" 
                                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" 
                                allowfullscreen>
                            </iframe>
                        </div>

                        <p><strong>الوصف:</strong> <%= video.description || 'لا يوجد وصف متاح.' %></p>
                    </div>
                <% }); %>
            <% } else { %>
                <p style="text-align: center; font-size: 1.2em; color: #aaa;">عذراً، لا تتوفر فيديوهات دراسية حالياً.</p>
            <% } %>
        </div>
    </main>
    
    <footer>
        <p>&copy; <%= new Date().getFullYear() %> BacTube. منصة دراسية مركزة.</p>
    </footer>
</body>
</html>
